# Medium Case

复杂度边界：输入配置经过配置模块和客户端模块，到达一个可观察请求描述；两到三个不可分离 Writes；一个集成测试 Seam。

正式需求待后续讨论。候选方向是让一个新配置值沿现有数据流传播，同时保留省略行为，不增加第二套配置对象或兼容回退。

目标：验证跨模块数据流规划、一个行为完整 Task、精确 Produces/Consumes 和集成 Slice Gate 的时间。
